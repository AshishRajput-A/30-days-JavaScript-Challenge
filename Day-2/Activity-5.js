// Ternary Operator

// Task-14 wap that uses the tempory operator to check is a number is positive or nrgative and log the result to the console.

let num = 20;

let result = num > 0 ? "Number is positive" : "Number is negative";

console.log(result);
