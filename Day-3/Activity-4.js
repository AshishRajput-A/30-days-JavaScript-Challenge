// conditional Ternory operator

// Task-6 wap that uses uses the ternory operator to check if number is even or odd and log the result to the console.

let num = 13;

let result = num % 2 == 0 ? "Number is even" : "Number is odd";

console.log(result);
